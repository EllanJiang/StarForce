/**
* @file:   excel\AttrDecay.xlsx\AttrDecay
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message AttrDecay{
	// key 
	uint32 id = 1;
	// 属性类型 
	uint32 attrId = 2;
	// 衰减时间(day) 
	uint32 decayTime = 3;
	// 衰减值 
	uint32 decayValue = 4;
	// 属性衰减截止时间 
	int64 decayEnd = 5;
	// 属性重置时间 
	int64 resetTime = 6;
	// 重置周期(day) 
	uint32 cycleDay = 7;
	// 周期结束时间 
	int64 cycleEnd = 8;
}

message AttrDecay_Array {
	repeated AttrDecay items = 1;
}
