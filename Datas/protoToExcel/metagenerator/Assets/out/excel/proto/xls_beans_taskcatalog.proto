/**
* @file:   excel\TaskCatalog.xlsx\TaskCatalog
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message TaskCatalog{
	// key 
	uint32 id = 1;
	// 活动名称 
	string name = 2;
	// 切页对应ID（具体配置在TaskMenu表中） 
	uint32 menu = 3;
	// 是否按照可领奖排序(会将可领奖的任务放在最新显示, 若都可领奖则按id排序) 
	bool sortByAward = 4;
	// “一键领取”按钮开关 
	bool allRewardFlag = 5;
	// 背景类型(详见说明) 
	uint32 backgroundType = 6;
	// 样式类型(详见说明) 
	uint32 displayType = 7;
	// 排序权重(不稳定排序，同一权重，配置表中靠后的在游戏中靠前) 
	uint32 sortWeight = 8;
	// 活动标题 
	string title = 9;
	// 活动标题填充参数 
	repeated string titleParam = 10;
	// 活动副标题 
	string subTitle = 11;
	// 活动副标题填充参数 
	repeated string subTitleParam = 12;
	// 活动描述 
	string desc = 13;
	// 活动描述填充参数 
	repeated string descParam = 14;
	// Banner图ID 
	uint32 bannerIconID = 15;
	// 背景图ID 
	uint32 bgIconID = 16;
	// 条目背景图ID 
	uint32 cellBgIcon = 17;
	// 对应TaskCatalogProgress表中的id列 
	uint32 progressID = 18;
	// 对应TaskCatalogProgress表中的id列 
	uint32 weekProgressID = 19;
	// 对应TaskCatalogMenu表中的id列 
	repeated uint32 menuIDs = 20;
}

message TaskCatalog_Array {
	repeated TaskCatalog items = 1;
}
