/**
* @file:   excel\LobbyVersionControl.xlsx\LobbyVersionControl
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message LobbyVersionControl{
	// 对应规则id 
	uint32 id = 1;
	// 服务器版本id 
	uint32 lobbyVersion = 2;
	// 最低版本号(0表示不限制) 
	string clientVersionMin = 3;
	// 最高版本号(0表示不限制) 
	string clientVersionMax = 4;
}

message LobbyVersionControl_Array {
	repeated LobbyVersionControl items = 1;
}
