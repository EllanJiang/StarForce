/**
* @file:   excel\ChannelConfigTable.xlsx\ChannelConfigTable
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message ChannelConfigTable{
	// 频道id 
	uint32 id = 1;
	// 频道类型 
	string channelType = 2;
	// 快捷回复id 
	uint32 quickChatMsgType = 3;
	// 频道标签颜色 
	string tagColor = 4;
	// 条目数上限 
	uint32 count = 5;
	// 频道名字 
	string channelName = 6;
	// 发送消息间隔 
	uint32 sendInterval = 7;
	// 频道解锁等级 
	uint32 unlockLevel = 8;
	// 时间戳间隔时间（单位：秒） 
	uint32 timeStampInterval = 9;
	// 日期显示格式化长样式 
	string timeStampLongFormat = 10;
	// 日期显示格式化短样式 
	string timeStampShortFormat = 11;
	// 消耗物ID 
	repeated ChannelConfigTable_cost cost = 12;
}

message ChannelConfigTable_cost{
	// 消耗物ID 
	uint32 id = 1;
	// 消耗数量 
	uint32 value = 2;
}

message ChannelConfigTable_Array {
	repeated ChannelConfigTable items = 1;
}
