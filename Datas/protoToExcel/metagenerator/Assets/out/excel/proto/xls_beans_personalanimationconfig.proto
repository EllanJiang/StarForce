/**
* @file:   excel\PersonalAnimationConfig.xlsx\PersonalAnimationConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message PersonalAnimationConfig{
	// id 
	uint32 id = 1;
	// 物品ID 
	uint32 itemId = 2;
	// 对应HallAnimResourcesConfig表中的id列 
	uint32 maleAnimID = 3;
	// 对应HallAnimResourcesConfig表中的id列 
	uint32 femaleAnimID = 4;
	// 动作播放间隔时长，填写大于0的数字时生效，小于等于0则为不循环 
	float timeInterval = 5;
	// 物品名称 
	string name = 6;
	// 图标ID 
	uint32 iconID = 7;
	// 是否是专属 
	bool isExclusive = 8;
	// 是否默认解锁 
	bool isDefault = 9;
	// 默认组队动作 
	bool teamAnimationDefault = 10;
	//  
	uint32 order = 11;
	// 品质 
	uint32 quality = 12;
}

message PersonalAnimationConfig_Array {
	repeated PersonalAnimationConfig items = 1;
}
