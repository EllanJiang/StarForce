/**
* @file:   excel\RegionServerConfig.xlsx\RegionServerConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message RegionServerConfig{
	// 服务器regionId 
	string id = 1;
	// 配置表表名后缀 
	string suffix = 2;
	// 时间偏移 
	sint32 timeOffset = 3;
	// 客户端展示id 
	int32 showId = 4;
}

message RegionServerConfig_Array {
	repeated RegionServerConfig items = 1;
}
