/**
* @file:   excel\ItemRewardsConfig.xlsx\ItemRewardsConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message ItemRewardsConfig{
	// id(主要用于日志记录) 
	uint32 id = 1;
	// 掉落方案号 
	uint32 rewardID = 2;
	// 概率类型 
	uint32 type = 3;
	// 概率参数（1kw） 
	uint32 value = 4;
	// 道具ID1、属性ID、特殊效果ID 
	repeated ItemRewardsConfig_effect effect = 5;
}

message ItemRewardsConfig_effect{
	// 道具ID1、属性ID、特殊效果ID 
	uint32 id = 1;
	// 数量、编号 
	uint32 value = 2;
	// 交易锁定时间（秒，0无限制） 
	uint32 lockTime = 3;
	// 有效时间（秒，0无限制） 
	uint32 expire = 4;
	// 是否暗金 
	uint32 statTrak = 5;
	// 指定皮肤编号 
	uint32 paintKit = 6;
}

message ItemRewardsConfig_Array {
	repeated ItemRewardsConfig items = 1;
}
