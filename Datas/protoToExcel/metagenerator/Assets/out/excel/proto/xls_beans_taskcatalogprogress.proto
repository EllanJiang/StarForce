/**
* @file:   excel\TaskCatalogProgress.xlsx\TaskCatalogProgress
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message TaskCatalogProgress{
	// 进度条id 
	uint32 id = 1;
	// 关联属性 
	uint32 attrID = 2;
	// 属性MAX值 
	uint32 attrMax = 3;
	// 进度任务ID 
	repeated TaskCatalogProgress_progress progress = 4;
}

message TaskCatalogProgress_progress{
	// 进度任务ID 
	uint32 id = 1;
	// 进度任务索引 
	uint32 index = 2;
}

message TaskCatalogProgress_Array {
	repeated TaskCatalogProgress items = 1;
}
