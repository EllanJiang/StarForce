/**
* @file:   excel\SkinAbilityConfig.xlsx\SkinAbilityConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message SkinAbilityConfig{
	// Ability id（数值功能ID） 
	uint32 id = 1;
	// 图标资源ID 
	uint32 IconResourceID = 2;
	// 品质 
	uint32 Quality = 3;
	// 前置条件  
	uint32 BuffEnterCondition = 4;
	// 退出条件 
	uint32 BuffExitCondition = 5;
	// 词条名 
	string BuffName = 6;
	// 词条内容说明（所有参数为整数类型 小数可以通过单位换算） 
	string BuffDes = 7;
	// buff参数(id 读buffdataconfig) 
	repeated uint32 BuffList = 8;
}

message SkinAbilityConfig_Array {
	repeated SkinAbilityConfig items = 1;
}
