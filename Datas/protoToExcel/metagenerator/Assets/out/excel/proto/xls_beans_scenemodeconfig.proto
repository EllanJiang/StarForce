/**
* @file:   excel\SceneModeConfig.xlsx\SceneModeConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message SceneModeConfig{
	// 地图显示ID（局外） 
	uint32 id = 1;
	// 地图玩法ID（局内） 
	uint32 idMS = 2;
	// 备注 
	string desc = 3;
	// 模式玩法ID 
	uint32 idMode = 4;
	// 场景ID 
	uint32 idScene = 5;
}

message SceneModeConfig_Array {
	repeated SceneModeConfig items = 1;
}
