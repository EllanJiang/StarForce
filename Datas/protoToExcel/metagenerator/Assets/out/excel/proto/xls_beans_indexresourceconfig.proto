/**
* @file:   excel\IndexResourceConfig.xlsx\IndexResourceConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message IndexResourceConfig{
	// 索引id 
	uint32 id = 1;
	// 路径 
	string path = 2;
	// 超低档机缓存数量 
	repeated int32 num = 3;
	// 超低档机缓存最大数量 
	repeated int32 maxNum = 4;
	// 读取需要预加载的模式的方式，白名单或是黑名单 
	bool isWhiteList = 5;
	// 预加载模式 
	repeated int32 mode = 6;
	// 特效清除时机 
	repeated uint32 clearFlag = 7;
	// 不限数量时最大缓存数倍数 
	int32 extremityMaxMultiple = 8;
}

message IndexResourceConfig_Array {
	repeated IndexResourceConfig items = 1;
}
