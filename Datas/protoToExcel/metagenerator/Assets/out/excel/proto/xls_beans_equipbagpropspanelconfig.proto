/**
* @file:   excel\EquipBagPropsPanelConfig.xlsx\EquipBagPropsPanelConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message EquipBagPropsPanelConfig{
	// 编号 
	uint32 id = 1;
	// 槽位ID（与EquipBagSlotConfig中ID对应） 
	uint32 slotID = 2;
	// 是否锁定 
	uint32 isLock = 3;
	// 锁定时显示的图标ID 
	uint32 iconID = 4;
}

message EquipBagPropsPanelConfig_Array {
	repeated EquipBagPropsPanelConfig items = 1;
}
