/**
* @file:   excel\InspectViewAnimConfig.xlsx\InspectViewAnimConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message InspectViewAnimConfig{
	// 武器角色id 
	uint32 id = 1;
	// Timeline动画资源全路径 
	string timelineAssetPath = 2;
	// 播放多少ms后可以主动打断（填0就是必须等播完） 
	uint32 enableBreakTime = 3;
}

message InspectViewAnimConfig_Array {
	repeated InspectViewAnimConfig items = 1;
}
