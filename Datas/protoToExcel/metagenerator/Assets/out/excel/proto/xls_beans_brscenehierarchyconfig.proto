/**
* @file:   excel\BRSceneHierarchyConfig.xlsx\BRSceneHierarchyConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message BRSceneHierarchyConfig{
	// 层级类别 
	int32 HierarchyType = 1;
	// 分块尺寸（-1不限） 
	int32 TileSize = 2;
	// 最小加载距离（大于等于此值时加载，-1不限） 
	int32 LoadMinDistanceLimit = 3;
	// 最大加载距离（小于此值时加载，-1不限） 
	int32 LoadMaxDistanceLimit = 4;
	// 最小加载高度（大于等于此值时加载，-1不限） 
	int32 LoadMinHeightLimit = 5;
	// 最大加载高度（小于此值时加载，-1不限） 
	int32 LoadMaxHeightLimit = 6;
	// 低端机是否加载 
	bool LoadLowDeviceLimit = 7;
	// 加载优先级（从低到高排序） 
	int32 LoadPriority = 8;
}

message BRSceneHierarchyConfig_Array {
	repeated BRSceneHierarchyConfig items = 1;
}
