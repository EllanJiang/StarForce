/**
* @file:   excel\AntiCheatRiskJudgeConfig.xlsx\AntiCheatRiskJudgeConfig
* @author: bommy
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package D11.Pbeans;

message AntiCheatRiskJudgeConfig{
	// 风险等级对应的配置 
	uint32 id = 1;
	// 触发时间周期（天） 
	uint32 cycleDay = 2;
	// 邮件标题 
	string sendMailTitle = 3;
	// 邮件正文 
	string sendMailText = 4;
	// 触发次数 
	repeated AntiCheatRiskJudgeConfig_judge judge = 5;
}

message AntiCheatRiskJudgeConfig_judge{
	// 触发次数 
	uint32 time = 1;
	// 特殊匹配并不加信用分惩罚天数 
	uint32 markDay = 2;
	// 直接扣至多少信用分 
	sint32 creditPointTarget = 3;
	// 清榜天数 
	uint32 leaderBoardDelDay = 4;
	// 封禁天数 
	uint32 blockDay = 5;
	// 是否踢出 
	uint32 isKick = 6;
}

message AntiCheatRiskJudgeConfig_Array {
	repeated AntiCheatRiskJudgeConfig items = 1;
}
